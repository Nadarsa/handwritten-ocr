# Requirements для проекта распознавания рукописного кириллического текста
# Handwritten OCR Project - MIPT x GreenData
# Python 3.8+

# CORE ML FRAMEWORKS
torch>=2.0.0,<2.3.0
torchvision>=0.15.0,<0.18.0

# TRANSFORMERS & OCR MODELS
# TrOCR модели (основная архитектура)
transformers>=4.35.0,<4.40.0
sentencepiece>=0.1.99

# PaddleOCR для детекции строк
paddleocr>=2.7.0,<2.9.0
paddlepaddle>=2.5.0,<3.0.0

# COMPUTER VISION & IMAGE PROCESSING
opencv-python>=4.8.0,<4.10.0
Pillow>=10.0.0,<11.0.0

# DATA AUGMENTATION
albumentations>=1.3.0,<1.5.0
imgaug>=0.4.0

# DATA PROCESSING
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0

# METRICS & EVALUATION
# CER/WER метрики
python-Levenshtein>=0.23.0
Levenshtein>=0.25.0
cer>=1.2.0
werpy>=3.2.0

# Дополнительные метрики
scikit-learn>=1.3.0,<1.5.0

# VISUALIZATION & ANALYSIS
matplotlib>=3.7.0,<3.10.0
seaborn>=0.12.0,<0.14.0
plotly>=5.18.0,<6.0.0

# Для статистического анализа
scipy>=1.10.0,<1.14.0

# JUPYTER & NOTEBOOKS
jupyter>=1.0.0
ipywidgets>=8.0.0
notebook>=7.0.0
ipykernel>=6.25.0

# UTILITIES
tqdm>=4.66.0
pyyaml>=6.0.0

# Google Drive integration для Colab
gdown>=4.7.0

# OPTIONAL DEPENDENCIES
# Experiment tracking
# wandb>=0.16.0
# mlflow>=2.10.0

# Model optimization
# onnx>=1.15.0
# onnxruntime>=1.16.0

# EasyOCR (если нужен для сравнения)
# easyocr>=1.7.0
# pytesseract>=0.3.10


# INSTALLATION INSTRUCTIONS

#
# Установка по умолчанию (с CUDA):
#   pip install -r requirements.txt
#
# Для CPU-only PyTorch (легковесная версия):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Для конкретной версии CUDA (например, 11.8):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# Для Google Colab (уже установлены torch, numpy):
#   pip install -r requirements.txt
#
# Минимальная установка только для инференса:
#   pip install -r requirements.inference.txt
#
# NOTES
#
# 1. PaddlePaddle может требовать специальной установки для GPU
#    См. https://www.paddlepaddle.org.cn/en/install/quick
#
# 2. Для M1/M2 Mac используйте:
#    pip install torch torchvision
#    (без --index-url)
#
# 3. Размер установки: ~3-5 GB
#
